apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: n8n
  namespace: n8n
spec:
  secretName: n8n-tls
  dnsNames:
    - n8n.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-production-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
