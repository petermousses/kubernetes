apiVersion: v1
kind: Service
metadata:
  name: librechat
  namespace: librechat
  labels:
    app.kubernetes.io/name: librechat
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: librechat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: librechat
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 3080
      targetPort: http
      protocol: TCP
