apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage
  namespace: homepage
spec:
  secretName: homepage-tls
  dnsNames:
    - home.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-production-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
