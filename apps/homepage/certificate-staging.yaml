apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-staging
  namespace: homepage
spec:
  secretName: homepage-tls-staging
  dnsNames:
    - home.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-staging-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
