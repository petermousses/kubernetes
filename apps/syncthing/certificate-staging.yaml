apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: syncthing-staging
  namespace: syncthing
  labels:
    app.kubernetes.io/name: syncthing
    app.kubernetes.io/part-of: syncthing
spec:
  secretName: syncthing-tls-staging
  dnsNames:
    - sync.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-staging-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io

