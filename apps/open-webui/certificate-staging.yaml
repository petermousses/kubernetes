apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: open-webui-staging
  namespace: open-webui
spec:
  secretName: open-webui-tls-staging
  dnsNames:
    - chat.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-staging-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
