apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: board-games-staging
spec:
  secretName: board-games-tls-staging
  dnsNames:
    - games.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-staging-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
