apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rancher
  namespace: cattle-system
  labels:
    app.kubernetes.io/name: rancher
spec:
  secretName: tls-rancher-ingress
  dnsNames:
    - rancher.omv.mousses.xyz
  issuerRef:
    name: letsencrypt-production-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
