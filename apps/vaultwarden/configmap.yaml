apiVersion: v1
kind: ConfigMap
metadata:
  name: vaultwarden-config
  namespace: vaultwarden
  labels:
    app.kubernetes.io/name: vaultwarden
    app.kubernetes.io/part-of: vaultwarden
data:
  # Preserve the application's public canonical URL from the Docker source.
  DOMAIN: https://vault.mousses.xyz
