# Kuberentes Inventory and Patterns

From the parent dir of this repo
- `kubernetes/` (this repo) contains declarative Kubernetes resources.
- `data/` is reserved for copied stack-local application data.

